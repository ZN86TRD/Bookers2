<!--投稿一覧-->
<% @books.each do |book| %>

    <div>
        <%=n image_tag book.get_image %>
        <p>投稿ユーザー画像: <%= image_tag "sample-author1.jpg" %></p>
        <p>Title<%= book.title %></p>
        <p>Opinion<%= book.body %></p>
        <p>ユーザーネーム:<%= book.user.name %></p>
    </div>

<% end %>